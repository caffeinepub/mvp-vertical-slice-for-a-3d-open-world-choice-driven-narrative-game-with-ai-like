## MVP vertical slice for a 3D open-world, choice-driven narrative game with AI-like NPC behaviors (original soap-opera-inspired setting)

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
